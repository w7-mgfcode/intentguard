# Artifact directory

U03 and U04 will write immutable, versioned baseline and transformer artifacts here. Generated weights and metadata are ignored by Git; this README is the tracked contract.

No trained model, selected threshold, revision hash, or performance claim exists during Gate A.
