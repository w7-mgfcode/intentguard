# Data directory

`make data` will populate this directory with locally generated BANKING77 cache and provenance outputs under U02. Generated contents are ignored by Git; this README is the tracked contract.

The dataset revision is deliberately `UNRESOLVED` in `configs/default.toml` until U02 verifies and records an immutable upstream revision. No BANKING77 dataset has been downloaded during Gate A.
